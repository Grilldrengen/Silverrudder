<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        xmlns:views="clr-namespace:UI.Views"
        xmlns:viewsmodels="clr-namespace:UI.ViewModels"
        mc:Ignorable="d"
        Title="Svendborg Sejlklub" Height="700" Width="1200">


  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height='1*' />
    </Grid.RowDefinitions>

    <TabControl Grid.Row='0'>
      <TabItem Header='Administrer Deltager'  Height='25'>
        <views:ManageParticipant />
      </TabItem>
      <TabItem Header='Administrer Kategorier' />
      <TabItem Header='Stop Bådenes Tider' />
    </TabControl>

  </Grid>
</Window>
